# This workflow updates the code line count badges displayed in the README file.
# The badges live on the badges branch; this workflow only rewrites the counts in them.

name: Badges

on:
  push:
    branches:
      - develop

jobs:
  badges:
    name: Update badges
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
      - uses: actions/checkout@v7
        with:
          ref: badges
          path: badges
      - run: node .github/scripts/update-badges.mjs badges
      - name: Commit the badges
        working-directory: badges
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git diff --quiet || { git commit -am "Update the code line count badges."; git push; }
